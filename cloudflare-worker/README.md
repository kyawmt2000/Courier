# Blink Courier API Proxy

This Cloudflare Worker keeps the backend on Render, but gives the apps a Cloudflare URL to call.

Origin:

https://courier-api-bq5c.onrender.com

Deploy this worker in Cloudflare, then use the Worker URL as the app API base URL.

